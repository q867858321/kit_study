1，对象删除弄个键
var obj={
    aa：1,bb:2
}
delete obj['aa']

2、提交输出重新编组
let params={};
for(let key in this.ruleForm){
    if(key=='aa'||key=='bb'){
        content;
    }else{
        params[key]=this.ruleForm[key];
    }
}

3.字符串以,分割,成为数组
1,2 => [1,2]
let newArr=arr.split(',')
4.数组以,分割为字符串
[1,2] => 1,2
let str=arr.join(',')

4.字符串转换为数值型
'123' => 123
parseInt("123") ; parseInt("123av") 123

5.数值型转为字符串
123 => '123'
123.toString(); 123+''

6.字符串中是否包含某字符串，数组中是否包含某一项
let num=‘hello world’.indexOf("world")
var fruits = ["Banana", "Orange", "Apple", "Mango"];
var a = fruits.indexOf("Apple"); 
