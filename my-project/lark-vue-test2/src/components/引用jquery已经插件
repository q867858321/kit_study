1.安装jquery
npm install jquery -s
2、配置jquery
在build/webpack.base.confg.js里
const webpack=require('webpack')
plugins:[
    new webpack.ProvidePlugin({
      $:'jquery',
      jQuery: "jquery"
    })
  ],
3、引用的地方引入（在static中）
import '../../static/lib/paging'; //引用jquery插件